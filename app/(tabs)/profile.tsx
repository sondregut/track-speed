/**
 * Profile Tab Screen
 *
 * User profile, stats, and quick access to settings.
 */

import { ProfileScreen } from '../../src/screens';

export default function ProfileTab() {
  return <ProfileScreen />;
}
