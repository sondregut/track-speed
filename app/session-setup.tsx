/**
 * Session Setup Screen
 *
 * Configure session type, distance, and start method.
 */

import { SessionSetupScreen } from '../src/screens';

export default function SessionSetup() {
  return <SessionSetupScreen />;
}
