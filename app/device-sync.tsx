/**
 * Device Sync Screen
 *
 * Connect multiple phones for multi-gate timing.
 */

import { DeviceSyncScreen } from '../src/screens';

export default function DeviceSync() {
  return <DeviceSyncScreen />;
}
