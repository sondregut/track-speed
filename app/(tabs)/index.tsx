/**
 * Home Tab Screen
 *
 * Entry point for the app - quick session setup and start timing.
 */

import { HomeScreen } from '../../src/screens';

export default function HomeTab() {
  return <HomeScreen />;
}
