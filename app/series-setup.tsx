/**
 * Series Setup Screen
 *
 * Configure interval/series training parameters.
 */

import { SeriesSetupScreen } from '../src/screens';

export default function SeriesSetup() {
  return <SeriesSetupScreen />;
}
